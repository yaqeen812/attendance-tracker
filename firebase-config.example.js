/*
  Firebase config example.
  Steps:
  1) Create a Firebase project in the Firebase console (https://console.firebase.google.com/)
  2) Add a web app to the project and copy the firebaseConfig object
  3) Save a new file named `firebase-config.js` (same folder) containing *only* the firebaseConfig object

  Example `firebase-config.js` contents:

  window.firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "...",
    appId: "..."
  };

  Keep this file private — do not commit real keys to public repositories.
*/

(function(){
  // This example file is intentionally inert. Create a real `firebase-config.js`
  // in the same folder with the `window.firebaseConfig = { ... }` object as shown above.
})();
